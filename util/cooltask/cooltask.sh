. shlib-import findcont

# Using `T <job-class> <job-id>` for `J` and `Jwhich`.
alias T='findcont %cooltask -C'
alias t='findcont %cooltask -C -A'
#alias J='findcont %cooljob -C'
alias T.='findcont %cooltask'
#alias Jwhich='findcont %cooljob'

alias Q='qlog'
