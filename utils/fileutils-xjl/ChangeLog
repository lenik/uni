2010-12-13  lenik

	* Makefile.am: include subst/rst/allin stuff in `coolauto.am`.
	* Makefile.am: include `allin.am` instead of setup rules for each
	  makefile.

	  using `do_subst += ...` to append more params.
	* Makefile.am: lock the generated file by `chmod -w`.

	  fix bad tab/space mixings
	* configure.ac: Add .rst implicit rules and set default
	  `-Wno-portability` in `configure.ac`

2010-11-30  lenik

	* .: add ignores

2010-11-16  lenik

	* enfile, rename-p, symflatten: Enable the getopt long options.
	  Using POSIXLY_CORRECT to restrict the options at the beginning of
	  cmdline.

2010-11-02  lenik

	* mv-p: enable gnu_getopt for those Getopts::Long.

2010-10-14  lenik

	* Makefile.am, VERSION.av, configure.ac: add missed tourl.

2010-10-10  lenik

	* coolmore, enfile, mv-p, rename-p, symflatten: keyword subst
	  updates.

2010-09-30  lenik

	* Makefile.am, VERSION.av, configure.ac, coolmore, fileutils.sh,
	  rename-p: add alias.

2010-09-29  lenik

	* Makefile.am, VERSION.av, configure.ac, mv-p, mv-p.1, rename-p,
	  rename-p.1: new mv-p written by perl.
	* VERSION.av, configure.ac, rename-p: add -a/-o condition rules
	* Makefile.am, VERSION.av, configure.ac, rename-p, rename-p.1: add
	  rename-p. (to separate snapshots/releases in m2repo)

2010-09-23  lenik

	* .: set ignore list.
	* .: ignore *.gz.

2010-09-22  lenik

	* debian/postinst, debian/prerm: pre/post now turns to auto
	  generated.

2010-09-21  lenik

	* swap: tab/space review.

2010-09-20  lenik

	* VERSION.av, configure.ac, enfile, symflatten: shlib-import
	  updates.

2010-09-18  lenik

	* VERSION.av, configure.ac, debian, debian/control, debian/docs,
	  debian/postinst, debian/prerm, enfile, symflatten: re-init.
	* tourl: move headrm, grepl to -coretext.
	  move tourl to -fileutils.
	  move sedit to -configutils.
	* ., AUTHORS, ChangeLog, Makefile.am, NEWS, README, VERSION.av,
	  binaryp, enfile, postinst.in, prerm.in, swap, symfl, symfl.1,
	  symflatten, textp: move file utilties to -fileutils.
