#!/bin/sh
# master-file

bashrc=@sysconfdir@/bash.bashrc
a_init=@sysconfdir@/bash_aliases
echo -n "Remove $a_init from start up... "
    if lineconf -ekt $bashrc COOLBASH::a_init; then
        echo Done
    else
        echo Skipped
    fi

exit 0
