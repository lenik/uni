2010-12-16  lenik

	* VERSION.av, configure.ac, tree-index: change > >(...) > >(...) =>
	  > >(... | ...)
	* Makefile.am, VERSION.av, bash_aliases.d/cd-tree,
	  bash_aliases.d/cd-tree.sh, configure.ac: alias script must ends
	  with `.sh`
	* ., Makefile.am, VERSION.av, bash_aliases.d/cd-tree,
	  bash_completion.d/cd-tree, configure.ac, tree-index, xf-tree: add
	  xft/vit/catt/xcatt stuff.
	* ., AUTHORS, ChangeLog, Makefile.am, NEWS, README, VERSION.av,
	  bash_aliases.d, bash_aliases.d/cd-tree, bash_completion.d,
	  bash_completion.d/cd-tree, cd-tree, configure.ac, debian,
	  debian/control, debian/docs, debian/postinst, debian/prerm,
	  postinst.in, prerm.in, tree-index: move cd-tree to -cdtree.
	  (Ref-Loop: cd-tree -> coolansi -> coolbash)
