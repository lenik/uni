#!/bin/bash
#! master-file

. shlib-import cliboot

LOGLEVEL=2

_log1 "Unconfigure Jetty"

    lineconf -ktm "  Unload archiva-jetty-config.xml" \
        @jettyconfdir@/jetty.conf load::archiva

_log1 "Unconfig apache from forward archiva"

    a2reload

exit 0

