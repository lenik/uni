2010-12-13  lenik

	* ., VERSION.av, bash_completion.d/hm, configure.ac, hm: cache the
	  package contents list, by the way.
	* VERSION.av, bash_completion.d/hm, configure.ac: hm cache the
	  package list.
	* configure.ac: Add .rst implicit rules and set default
	  `-Wno-portability` in `configure.ac`

2010-12-09  lenik

	* Makefile.am, VERSION.av, bash_completion.d, bash_completion.d/hm,
	  configure.ac, hm, hm-subhist: add hm completion feature.

2010-11-16  lenik

	* collect-copy, hm, kill-all: Enable the getopt long options.
	  Using POSIXLY_CORRECT to restrict the options at the beginning of
	  cmdline.

2010-11-15  lenik

	* VERSION.av, configure.ac, kill-all: typo log2/_log2
	* VERSION.av, configure.ac, kill-all: filter the grepped result of
	  ps, so that the self program won't be killed.
	* VERSION.av, configure.ac, kill-all: kill-all GREP-PATTERN

2010-11-02  lenik

	* VERSION.av, configure.ac, debian/control: depends libglib-2.0-0
	* VERSION.av, configure.ac, debian/control: add depends:
	  libglib2.0-dev and libglib2.0

2010-10-30  lenik

	* VERSION.av, configure.ac: newmpl bash.sh, perl.pl should be
	  executable, dist them in _SCRIPTS.

	  -coolmake depends on make.

	  -cools-term by Recommends rather then Depends.

	  new treetex (still under PLAN.)

	  -cooltex: cpan Text::Diff in postinst.

2010-10-25  lenik

	* Makefile.am, VERSION.av, configure.ac: valsum ignore for
	  temporary.

2010-10-23  lenik

	* debian/control: provides short package names to hidden xjl-
	  prefix.

2010-10-19  lenik

	* Makefile.am, configure.ac, valsum.c, valsum.sh: set ignores

2010-10-10  lenik

	* Makefile.am, VERSION.av, collect-copy, configure.ac, hm,
	  kill-all: add kill-all

2010-10-08  lenik

	* VERSION.av: cascade minor->commit

2010-09-23  lenik

	* .: set ignore list.
	* .: ignore *.gz.

2010-09-21  lenik

	* which2: tab/space review.

2010-09-20  lenik

	* VERSION.av, collect-copy, configure.ac, hm: shlib-import updates.

2010-09-18  lenik

	* collect-copy, debian/control, hm: shlib updates.
	* Makefile.am, grepl, headrm, sedit, sedit.1, tourl: move headrm,
	  grepl to -coretext.
	  move tourl to -fileutils.
	  move sedit to -configutils.
	* Makefile.am, binaryp, enfile, swap, textp: move file utilties to
	  -fileutils.
	* Makefile.am, debian/control, df-x, lineconf, symfl, symfl.1,
	  symflatten: rename -coolfsutils to -fsutils.

	  move df-x to -fsutils.

	  move lineconf to -coolbash.

	  create new -fileutils, and move sym* to fileutils.

2010-09-16  lenik

	* debian: ignore files under debian by default.
	* debian, debian/control, debian/docs: add debian control files.

2010-09-14  lenik

	* Makefile.am, VERSION.av, configure.ac, lineconf, sedit: sedit:
	  add --keep-all option

	  lineconf: change twoline/prefix to magic-text/magic-prefix, add
	  -l# option to support multiple line conf.

2010-09-09  lenik

	* Makefile.am, VERSION.av, configure.ac, sedit: set keep-mode
	  default.

2010-09-08  lenik

	* Makefile.am, VERSION.av, collect-copy, collect-copy.1,
	  configure.ac: add collect-copy to collect postinst/prerm from deb
	  sources.
	* Makefile.am, VERSION.av, configure.ac, symfl, symfl.1,
	  symflatten: add symflatten and symfl for batch.

2010-09-07  lenik

	* VERSION.av, configure.ac, enfile: fixed: disfile .disabled/*
	* VERSION.av, configure.ac, enfile: move to .disabled/ if the dir
	  exists, or add '.' to the decorated file, so as to hide away.
	* VERSION.av, configure.ac, enfile, random.c: fix pointer type in
	  random.c

	  set .hidden if disfile, and add suffix _INVALID, to away from
	  ext-wildcards.
	* VERSION.av, configure.ac, enfile: apply bash template to enfile.
	* Makefile.am, VERSION.av, configure.ac, enfile: add enfile to
	  enable/disable a file. (helper for apt/sources.list.d/xxx)
	* VERSION.av, configure.ac, lineconf, sedit: refactor option
	  --error-edit to --edit-status, and inverse the exit code to 0 if
	  anything changes.
	* VERSION.av, configure.ac, lineconf: lineconf return true if
	  something changed.
	* configure.ac, lineconf: adjust default loglevel.
	* configure.ac, lineconf: create new file if not existed.
	* configure.ac: autoverificated.
	* Makefile.am, binaryp, grepl, lineconf, sedit, textp, tourl:
	  transfer textp, binaryp, tourl from -coolbash.

	  new lineconf to do configuration by magic pattern.

2010-08-25  lenik

	* Makefile.am, config.h.in, configure.ac, random.c: add random.
	  (the rdev feature hasn't implemented yet)
	* Makefile.am, hm, hm.1: add hm (html manual) for search & open
	  html documents.

2010-08-24  lenik

	* Makefile.am: setup symlinks in install-data-hook, otherwise
	  -exec-hook fails cuz the dirs may be not created.
	* Makefile.am, configure.ac, gcd.c, mount-vex, mountall-alt,
	  umount-alt-a, umount-vex: move *mount* to -coolfsutils.

	  add gcd to calculate greatest common divisor.
	* ., configure.ac:

2010-08-23  lenik

	* add man page for uc, lc, to-xxx stuff.
	* implemented uc (and also lc, to-xxx)
	* new headrm to invert the `head' selection.
	  (fixed the pipe/process problem. )

	  new grepl to return the line number of matched.

	  add grepl usage example in sedit.1
	* sedit: utilized cliboot, and add options to diff, dry-run,
	  keep-xxx, etc.
	* set svn:executable on executables. add sedit for in-place edit &
	  save.

2010-08-20  lenik

	* autoconfiscated

2010-08-19  lenik

	* Transferred XT-utils to xjl-coolz, -coolutil, -coolhex.
