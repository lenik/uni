2010-12-13  lenik

	* Makefile.am: include subst/rst/allin stuff in `coolauto.am`.
	* Makefile.am: include `allin.am` instead of setup rules for each
	  makefile.

	  using `do_subst += ...` to append more params.
	* Makefile.am: lock the generated file by `chmod -w`.

	  fix bad tab/space mixings
	* configure.ac: Add .rst implicit rules and set default
	  `-Wno-portability` in `configure.ac`

2010-12-08  lenik

	* VERSION.av, configure.ac, sshproxy.conf.in: using autossh to
	  create proxy connection

2010-11-28  lenik

	* .: don't use axel as default wget.

2010-11-15  lenik

	* VERSION.av, configure.ac, sshproxy.conf.in: sshproxy default
	  listen on all interfaces.

2010-09-27  lenik

	* VERSION.av, configure.ac, postinst-client.sh.in: missing trailing
	  \
	* VERSION.av, configure.ac, postinst-client.sh.in: in
	  proxychains.conf: socks4 \t 127...
	  notice the tabchar.
	* VERSION.av, configure.ac, postinst-client.sh.in,
	  sshproxy.conf.in: /etc/proxychains -> /etc/proxychains.conf
	* VERSION.av, configure.ac, debian/control, postinst-client.sh.in:
	  add proxychains binds.

2010-09-23  lenik

	* .: set ignore list.
	* .: ignore *.gz.

2010-09-21  lenik

	* sshproxy.conf.in: tab/space review.

2010-09-20  lenik

	* VERSION.av, configure.ac, debian/control, postinst-base.sh.in,
	  postinst-client.sh.in: shlib-import updates.
	* VERSION.av, configure.ac, postinst-client.sh.in: Disable hash of
	  known hosts.
	* VERSION.av, configure.ac, postinst-base.sh.in: remove entire
	  existing HOME.

2010-09-19  lenik

	* VERSION.av, configure.ac, debian/control: depends on
	  ssh-server/client by package.

2010-09-17  lenik

	* Makefile.am: coolauto: add sysconfdir etc. more substs.

2010-09-16  lenik

	* debian: ignore files under debian by default.
	* debian, debian/control, debian/docs,
	  debian/xjl-sshproxy-base.install,
	  debian/xjl-sshproxy-base.postinst,
	  debian/xjl-sshproxy-base.prerm,
	  debian/xjl-sshproxy-client.install,
	  debian/xjl-sshproxy-client.postinst,
	  debian/xjl-sshproxy-client.prerm,
	  debian/xjl-sshproxy-server.install,
	  debian/xjl-sshproxy-server.postinst,
	  debian/xjl-sshproxy-server.prerm: add debian control files.
	* postinst-client.sh.in, prerm-client.sh.in: start sshproxy after
	  installed.
	* VERSION.av, configure.ac, postinst-base.sh.in, sshproxy.conf.in:
	  user sshproxy shell=/bin/false, and ssh into with -N option.
	* VERSION.av, configure.ac, postinst-base.sh.in: .ssh should be
	  700, not 600
	* VERSION.av, preinst-base.sh.in: mistake add.
	* postinst-base.sh.in: using bash instead.
	* Makefile.am, VERSION.av: sshproxy.conf was missing.
	* Makefile.am, configure.ac, post-install.sh.in,
	  postinst-base.sh.in, postinst-client.sh.in,
	  postinst-server.sh.in, pre-remove.sh.in, preinst-base.sh.in,
	  prerm-base.sh.in, prerm-client.sh.in, prerm-server.sh.in: group
	  -sshproxy-client into -sshproxy.

	  rearrange postinst/rm scripts.
	* ., Makefile.am, VERSION.av, authorized_keys, configure.ac,
	  id_rsa, post-install.sh.in, postinst-client.sh.in,
	  sshproxy.conf.in, sshproxy.pub: group sshproxy-server/client into
	  one -sshproxy.

2010-09-15  lenik

	* config sshproxy auth-keys
	* Initial by newdeb
