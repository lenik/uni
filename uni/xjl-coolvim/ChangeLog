2010-12-14  lenik

	* Makefile.am, VERSION.av, configure.ac, coolvim, find-prefix.c,
	  reload-file, reload-file.in, test, test/a.txt:
	  coolvim/reload-file done.
	* ., Makefile.am, VERSION.av, config.h.in, configure.ac,
	  find-prefix.c, reload-file, reload.cfg: add
	  reload-file/reload.cfg used by coolvim
	  add find-prefix used by reload-file.

2010-12-13  lenik

	* Makefile.am: include subst/rst/allin stuff in `coolauto.am`.
	* Makefile.am: include `allin.am` instead of setup rules for each
	  makefile.

	  using `do_subst += ...` to append more params.
	* Makefile.am: lock the generated file by `chmod -w`.

	  fix bad tab/space mixings
	* configure.ac: Add .rst implicit rules and set default
	  `-Wno-portability` in `configure.ac`

2010-12-04  lenik

	* VERSION.av, configure.ac, coolvim: `sudo head`.
	* VERSION.av, configure.ac, coolvim: sudo chmod.

2010-11-29  lenik

	* VERSION.av, aliases.d/coolvim.sh, configure.ac, coolvim: using
	  env `vim` instead of `VIM` to _switch **vim** application_.

	  auto set executable bit if file starts with '#!...'
	* VERSION.av, configure.ac, coolvim: env `VIM` must be reserved. so
	  use `vim` instead.
	* VERSION.av, aliases.d/coolvim.sh, configure.ac, coolvim: add
	  alias V=gvim/sudo

2010-11-28  lenik

	* .: don't use axel as default wget.

2010-11-16  lenik

	* VERSION.av, configure.ac, guesstab: $'\T' -> $'\t'
	* VERSION.av, configure.ac, guesstab: tab/space in the source code
	  is broken.. using $'\t' strictly.

2010-10-23  lenik

	* debian/control: provides short package names to hidden xjl-
	  prefix.

2010-10-20  lenik

	* VERSION.av, configure.ac, coolvim: add support to filedir/.vimrc

2010-09-23  lenik

	* .: set ignore list.
	* .: ignore *.gz.

2010-09-22  lenik

	* debian/postinst: pre/post now turns to auto generated.

2010-09-21  lenik

	* guesstab: tab/space review.
	* VERSION.av, configure.ac, guesstab: tab/space review. always
	  expand-tab in mixed-mode.
	* Makefile.am, VERSION.av, aliases.d/coolvim.sh, configure.ac,
	  postinst.in, prerm.in, vimrc.cool: add vimrc.cool, replace the
	  user .vimrc

2010-09-17  lenik

	* Makefile.am: libauto updates.
	* Makefile.am, VERSION.av, configure.ac: subst in am.
	* Makefile.am, VERSION.av, configure.ac, postinst.in, prerm.in:
	  include postinst.in in am.
	* VERSION.av, configure.ac, coolvim, debian/postinst, ifedit,
	  postinst.in: chmod sudoers/env_editor in postinst.
	* Makefile.am, VERSION.av, configure.ac, postinst.in: do chmod on
	  env_stuff to 440

2010-09-16  lenik

	* debian: ignore files under debian by default.
	* debian, debian/control, debian/docs: add debian control files.

2010-09-07  lenik

	* VERSION.av, configure.ac: 0.1
	* VERSION.av: autoverifiscated

2010-09-03  lenik

	* coolvim: sudo egrep if file isn't readable.

2010-08-25  lenik

	* Makefile.am, sudoers.d, sudoers.d/env_editor: config sudoers to
	  using vi as default editor.

2010-08-24  lenik

	* Makefile.am, profile.d, profile.d/coolvim.sh: add EDITOR=vi to
	  profile.d
	* coolvim: sudo when create file in non-writable directory, if
	  necessarily.

2010-08-21  lenik

	* aliases.d/coolvim.sh: add vis=coolvims
	* Makefile.am, configure.ac, coolvim: using install-hook to install
	  symlinks. and coolvims to enable sudo

2010-08-20  lenik

	* AUTHORS, ChangeLog, Makefile.am, NEWS, README, aliases.d,
	  aliases.d/coolvim.sh, configure.ac, coolvim, guesstab, viw: New
	  coolvim to take over viw, sudovi etc.

2010-08-19  lenik

	* .: Transferred XT-utils to xjl-coolz, -coolutil, -coolhex.

2010-08-18  lenik

	* add xjl-cooledit, -coolsync (from msync utils)

	  refine xjl-startup to xjl-cooltruecrypt
