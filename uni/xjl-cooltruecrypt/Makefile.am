include libauto/subst.am

dist_bin_SCRIPTS = \
    mount.tc \
    mountall.tc \
    nextdevid

man_MANS = \
    mount.tc.1 \
    fstab.tc.5 \
    passwd.tc.5 \
    nextdevid.1

sysconfdir = /etc
initdir = ${sysconfdir}/init

# permission??
sysconf_DATA = \
    fstab.tc \
    passwd.tc

init_DATA = \
    mount-tc.conf
