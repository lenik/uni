2010-12-13  lenik

	* Makefile.am: include subst/rst/allin stuff in `coolauto.am`.
	* Makefile.am: include `allin.am` instead of setup rules for each
	  makefile.

	  using `do_subst += ...` to append more params.
	* Makefile.am: lock the generated file by `chmod -w`.

	  fix bad tab/space mixings
	* configure.ac: Add .rst implicit rules and set default
	  `-Wno-portability` in `configure.ac`

2010-12-09  lenik

	* VERSION.av, configure.ac, debian/control: `Provides a, b` instead
	  of 2 Provides line.

2010-11-30  lenik

	* .: add ignores

2010-11-16  lenik

	* fstab-install.tc.in, passwd-install.tc.in: Enable the getopt long
	  options.
	  Using POSIXLY_CORRECT to restrict the options at the beginning of
	  cmdline.

2010-10-23  lenik

	* debian/control: provides short package names to hidden xjl-
	  prefix.

2010-10-21  lenik

	* VERSION.av, configure.ac, postinst.in, prerm.in: config max_loop
	  in grub defaults

2010-10-10  lenik

	* fstab-install.tc.in, passwd-install.tc.in: keyword subst updates.
	* VERSION.av, configure.ac, postinst.in: Add more loop devices

2010-09-27  lenik

	* VERSION.av, configure.ac, fstab-install.tc.in,
	  passwd-install.tc.in: sedit -k not -A.

2010-09-26  lenik

	* VERSION.av, configure.ac, fstab-install.tc.in,
	  passwd-install.tc.in: sedit -a -> -A

2010-09-23  lenik

	* ., test: set ignore list.
	* .: ignore *.gz.

2010-09-22  lenik

	* debian/postinst, debian/prerm: pre/post now turns to auto
	  generated.

2010-09-21  lenik

	* nextdevid: tab/space review.

2010-09-20  lenik

	* VERSION.av, configure.ac, fstab-install.tc.in,
	  passwd-install.tc.in: shlib-import updates.

2010-09-18  lenik

	* debian/control, fstab-install.tc.in, passwd-install.tc.in: shlib
	  updates.

2010-09-17  lenik

	* Makefile.am: coolauto: add sysconfdir etc. more substs.
	* VERSION.av, configure.ac, fstab-install.tc.in,
	  passwd-install.tc.in: lineconf non-error status.
	* Makefile.am, debian/postinst, post-install.sh.in, postinst.in,
	  pre-remove.sh.in, prerm.in: rename pre-remove/post-install to
	  short postinst. etc.

2010-09-16  lenik

	* debian: ignore files under debian by default.
	* debian, debian/control, debian/docs, debian/postinst,
	  debian/prerm: add debian control files.

2010-09-14  lenik

	* Makefile.am, VERSION.av, configure.ac, test/test.sh: add test.
	* Makefile.am, configure.ac, fstab-install.tc.in,
	  install-truecrypt.sh, mount.tc, mount.tc.in, mountall.tc,
	  mountall.tc.in, passwd-install.tc.in, post-install.sh.in,
	  pre-remove.sh.in, test, test/Makefile.am, test/fstab-delta,
	  test/fstab.tc.0, test/passwd-delta, test/passwd.tc.0: add
	  install-truecrypt.sh on the fly.
	  add fstab/passwd-install.tc which add entries by incremental.
	  add test scripts.

2010-09-08  lenik

	* Makefile.am, VERSION.av, configure.ac: rename upstartconfdir to
	  initdir.

2010-09-07  lenik

	* VERSION.av: autoverifiscated

2010-08-20  lenik

	* AUTHORS, ChangeLog, Makefile, Makefile.am, NEWS, README,
	  configure.ac: autoconfiscated

2010-08-18  lenik

	* Makefile: rename mount-tclams to mountall.tc
	  rename mount-tclams.conf to mount-tc.conf
	* mount-tc.conf, mount-tclams, mount-tclams.conf, mountall.tc:
	  rename mount-tclams to mountall.tc
	  rename mount-tclams.conf to mount-tc.conf
	* .: add xjl-cooledit, -coolsync (from msync utils)

	  refine xjl-startup to xjl-cooltruecrypt
	* init xjl- packages
