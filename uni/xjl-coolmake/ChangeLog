2010-12-13  lenik

	* Makefile.am: include subst/rst/allin stuff in `coolauto.am`.
	* Makefile.am: include `allin.am` instead of setup rules for each
	  makefile.

	  using `do_subst += ...` to append more params.
	* Makefile.am: lock the generated file by `chmod -w`.

	  fix bad tab/space mixings
	* configure.ac: Add .rst implicit rules and set default
	  `-Wno-portability` in `configure.ac`

2010-11-16  lenik

	* autobuild, makeorig: Enable the getopt long options.
	  Using POSIXLY_CORRECT to restrict the options at the beginning of
	  cmdline.

2010-11-03  lenik

	* VERSION.av, configure.ac, debian/control, makelook.in: using
	  treetex to generate itree.tex.

2010-10-30  lenik

	* debian/control: newmpl bash.sh, perl.pl should be executable,
	  dist them in _SCRIPTS.

	  -coolmake depends on make.

	  -cools-term by Recommends rather then Depends.

	  new treetex (still under PLAN.)

	  -cooltex: cpan Text::Diff in postinst.

2010-10-23  lenik

	* debian/control: provides short package names to hidden xjl-
	  prefix.

2010-10-11  lenik

	* Makefile.am, VERSION.av, autobuild, configure.ac, test,
	  test/Makefile: new autobuild, originally written for pdfmon.

2010-10-10  lenik

	* makeorig: keyword subst updates.

2010-10-06  lenik

	* VERSION.av, configure.ac, maketest: TAB char fix.
	* VERSION.av, configure.ac, coolmake.sh: add alias mlk=makelook

2010-09-23  lenik

	* .: set ignore list.
	* .: ignore *.gz.

2010-09-21  lenik

	* maketest, samples/Makefile: tab/space review.

2010-09-20  lenik

	* VERSION.av, configure.ac, makeorig: shlib-import updates.

2010-09-18  lenik

	* debian/control, makeorig: shlib updates.

2010-09-17  lenik

	* Makefile.am: coolauto: add sysconfdir etc. more substs.

2010-09-16  lenik

	* debian: ignore files under debian by default.
	* debian, debian/control, debian/docs: add debian control files.

2010-09-14  lenik

	* Makefile.am, VERSION.av, configure.ac, coolmake.sh, makelook.in:
	  tree -a

2010-09-02  lenik

	* VERSION.av, configure.ac, makeorig: test for unconf typo.
	* VERSION.av, configure.ac, makeorig: set default zip=gzip and
	  separator using -.

2010-09-01  lenik

	* VERSION.av, configure.ac, makeorig: change the default suffix
	  -orig to .orig.
	* VERSION.av, configure.ac, makeorig: reconf & unconf when desired.
	* Makefile.am, VERSION.av, configure.ac, makelook.in, makeorig:
	  makelook get the dist archive by DIST_ARCHIVES make var.

	  makeorig by make temporary install and compress it.
	* VERSION.av, configure.ac, makeorig: new makeorig to create a
	  -orig.tar.x tarball in parent dir.

2010-08-31  lenik

	* Makefile.am, make-color, makelook, makelook.in: add make-color,
	  and use it by default in makelook.
	* makelook: trigger autoreconf2, when configure file doesn't exist.

2010-08-21  lenik

	* Makefile.am, coolmake.sh: add aliases
	* Makefile.am, maketestvar: add maketestvar shortcut

2010-08-20  lenik

	* ., AUTHORS, ChangeLog, Makefile.am, NEWS, README, configure.ac,
	  makelook, maketest, samples, samples/Makefile, subdirs: init
