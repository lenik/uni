2010-12-13  lenik

	* Makefile.am: include subst/rst/allin stuff in `coolauto.am`.
	* Makefile.am: include `allin.am` instead of setup rules for each
	  makefile.

	  using `do_subst += ...` to append more params.
	* Makefile.am: lock the generated file by `chmod -w`.

	  fix bad tab/space mixings
	* configure.ac: Add .rst implicit rules and set default
	  `-Wno-portability` in `configure.ac`

2010-12-09  lenik

	* Makefile.am, VERSION.av, colorlog, configure.ac, debian/control:
	  add colorlog to highlight error/warn/info/debug lines for common
	  logs.

2010-11-30  lenik

	* .: add ignores

2010-11-18  lenik

	* Makefile.am, VERSION.av, ansifx, colors, configure.ac: rewrite
	  colors and rename to ansifx.

2010-11-03  lenik

	* Makefile.am, VERSION.av, configure.ac, settermtitle, setwintitle:
	  set term title support.

2010-10-23  lenik

	* debian/control: provides short package names to hidden xjl-
	  prefix.

2010-10-19  lenik

	* Makefile.am, VERSION.av, configure.ac, debian/control, meter: add
	  meter to display column indexes.

2010-09-23  lenik

	* .: set ignore list.
	* .: ignore *.gz.

2010-09-22  lenik

	* debian/postinst, debian/prerm: pre/post now turns to auto
	  generated.

2010-09-21  lenik

	* colors: tab/space review.

2010-09-17  lenik

	* Makefile.am, debian/postinst, debian/prerm, post-install.sh.in,
	  postinst.in, pre-remove.sh.in, prerm.in: rename
	  pre-remove/post-install to short postinst. etc.

2010-09-16  lenik

	* debian: ignore files under debian by default.
	* debian, debian/control, debian/docs, debian/postinst,
	  debian/prerm: add debian control files.
	* ., Makefile.am, VERSION.av, configure.ac, get-ansifilter,
	  post-install.sh.in, pre-remove.sh.in: rename -coolterm to
	  -coolansi

	  add ansifilter (build on the fly) to -coolansi.

2010-09-07  lenik

	* autoverifiscated
