2010-12-13  lenik

	* Makefile.am, VERSION.av, configure.ac, new.in: include
	  subst/rst/allin stuff in `coolauto.am`.
	* Makefile.am, VERSION.av, configure.ac, new.in,
	  newtmpl.d/Makefile.am, newtmpl.d/man.rst: add man section group
	  text.
	* Makefile.am, new.in: include `allin.am` instead of setup rules
	  for each makefile.

	  using `do_subst += ...` to append more params.
	* Makefile.am: lock the generated file by `chmod -w`.

	  fix bad tab/space mixings
	* VERSION.av, configure.ac, new.in, newtmpl.d/Makefile.am,
	  newtmpl.d/bash.sh, newtmpl.d/cprog.c, newtmpl.d/man.rst,
	  newtmpl.d/perl.pl, newtmpl.d/perllib.pm: add man.rst in
	  reStrucText, to replace the old plain `newman` util.

	  add support of more automatic parameters (`_*`).
	* configure.ac: Add .rst implicit rules and set default
	  `-Wno-portability` in `configure.ac`

2010-11-02  lenik

	* new.in, newtmpl.d/perl.pl: enable gnu_getopt for those
	  Getopts::Long.

2010-10-30  lenik

	* newtmpl.d/Makefile.am: newmpl bash.sh, perl.pl should be
	  executable, dist them in _SCRIPTS.

	  -coolmake depends on make.

	  -cools-term by Recommends rather then Depends.

	  new treetex (still under PLAN.)

	  -cooltex: cpan Text::Diff in postinst.

2010-10-23  lenik

	* debian/control: provides short package names to hidden xjl-
	  prefix.

2010-10-19  lenik

	* VERSION.av, configure.ac, newtmpl.d/Makefile.am,
	  newtmpl.d/perllib.pm: add perllib.pm template
	* ., newtmpl.d, newtmpl.d/Makefile.am: set ignores

2010-10-14  lenik

	* Makefile.am, VERSION.av, configure.ac, newtmpl.d,
	  newtmpl.d/Makefile.am, newtmpl.d/cprog.c, newtmpl.d/cprog.h,
	  newtmpl.d/cprogtest, newtmpl.d/cprogtest/configure.ac: new
	  cprog.c template for writing small C utilities
	* newtmpl.d/cprogtest, newtmpl.d/cprogtest/AUTHORS,
	  newtmpl.d/cprogtest/ChangeLog, newtmpl.d/cprogtest/Makefile.am,
	  newtmpl.d/cprogtest/NEWS, newtmpl.d/cprogtest/README,
	  newtmpl.d/cprogtest/VERSION.av, newtmpl.d/cprogtest/config.h.in,
	  newtmpl.d/cprogtest/configure.ac, newtmpl.d/cprogtest/cprog.c,
	  newtmpl.d/cprogtest/cprog.h: test package for cprog

2010-10-10  lenik

	* new.in, newtmpl.d/perl.pl: keyword subst updates.

2010-09-29  lenik

	* VERSION.av, configure.ac, newtmpl.d/perl.pl: perl exit if
	  GetOptions fails.
	* VERSION.av, configure.ac, newtmpl.d/bash.sh, newtmpl.d/perl.pl:
	  help Case changes.

2010-09-23  lenik

	* .: set ignore list.
	* .: ignore *.gz.

2010-09-21  lenik

	* new.in: tab/space review.
	* Makefile.am, VERSION.av, configure.ac, debian/control, new.in,
	  postinst: create symlinks for new-*.
	* VERSION.av, configure.ac, new.in, newtmpl.d/perl.pl: rewrite
	  new.in using perl.
	  add perl.pl template.
	* VERSION.av, configure.ac, new.in, newtmpl.d/bash.ini,
	  newtmpl.d/bash.sh, test, test/templ1: the new/generator will be
	  rewrite using perl.

2010-09-20  lenik

	* VERSION.av, configure.ac, new.in, newtmpl.d/bash.sh: don't do
	  sed, if nothing to subst.

2010-09-19  lenik

	* VERSION.av, configure.ac, newtmpl.d/bash.sh: shlib updates in
	  bash.sh template.

2010-09-18  lenik

	* debian/control, newtmpl.d/bash.sh: shlib updates.
	* VERSION.av, configure.ac, new.in: shlib-conv.
	* Makefile.am, VERSION.av, configure.ac, new.in: read params from
	  tty.
	  chmod only if not stdout.
	* Makefile.am, VERSION.av, configure.ac, generate.1, generate.in,
	  new.1, new.in, newtmpl.d, newtmpl.d/Makefile.am,
	  newtmpl.d/bash.ini, newtmpl.d/bash.sh, templates: rename
	  generate* to new*
	  rename templates/ to newtmpl.d/

2010-09-17  lenik

	* post-install.sh, postinst: rename pre-remove/post-install to
	  short postinst. etc.

2010-09-16  lenik

	* debian: ignore files under debian by default.
	* debian, debian/control, debian/docs: add debian control files.
	* Makefile.am: subst.am -> coolauto.am

2010-09-07  lenik

	* VERSION.av, configure.ac: 0.1
	* VERSION.av: autoverifiscated

2010-08-25  lenik

	* generate.in, templates/Makefile.am, templates/bash.sh: add force
	  overwrite option.
	  chmod --reference as the templates.
	* Makefile.am, configure.ac, generate.in, templates/Makefile.am,
	  templates/bash.sh: Initial release.
	* ., AUTHORS, ChangeLog, Makefile.am, NEWS, README, configure.ac,
	  generate.1, generate.in, post-install.sh, templates: init
