
2007-08-26
  * bin/ydump.pl:
	initial

  * bin/loop2.pl:
	clarify the usage of --change-file. also find script-file in PATH, if necessary.

  * bin/runx.pl, lib/cmt/path.pm:
	move runx::find_path to cmt::path::which

  * lib/cmt/win32.pm:
	clear_screen by scroll window to home cursor

  * lib/cmt/util.pm:
	eval failure message in $@ (rather than $!)

  * bin/.pl:
	main() is conflict with main:: package.

2007-08-25
  * abc.d/bin/: edwin.bat, mitc.bat, mits.bat, no.bat, ocamlw.bat, e.t.c...
	start bg-mode for windows.

  * abc.d/bin/: scilab.bat, .erl_init.bat, edwin.bat, erl.bat, e.t.c...
	add installs

  * bin/zero.bat:
	rewrite, using dd

  * .cvsignore, .env.as, bin/lapiota-init.bat, bin/start-apps.bat, e.t.c...
	init profile.d

  * bin/nfs.pl:
	add --wait-close to interact with autohotkey.

2007-08-24
  * bin/findabc.bat:
	auto chdir if called as `findabc prefix/'

  * bin/.bat:
	using `exit /b CODE' instead of `goto end'

  * abc.d/bin/: gcl.bat, sml.bat:
	find home by findabc.

  * bin/lapiota-init.bat:
	using a: as ~(HOME) directory

  * .cvsignore, .env.as, __LAPIOTA__, etc/hotkey, etc/sysdict, e.t.c...
	The Beginning Of Kachori Lapiota.

  * bin/lapiota-init.bat:
	add init-cygwin(3)

  * abc.d/bin/em.bat, abc.d/bin/plts.bat, bin/sml.bat:
	find home by findabc.

  * abc.d/bin/em.bat, abc.d/bin/gcl.bat, bin/.bat:
	borrowed the test of end-of-arglist from args.bat

  * bin/args.bat:
	test end-of-arglist using "-translate method. (still not complete)

  * bin/export.bat:
	export variable(s) from setlocal/endlocal block (replace the 'endlocal' by '%leave%')
	(now only export of single variable is supported.)

  * bin/ugrep.bat:
	grep -abo to print the byte offset, however the offset of the beginning of the matched null-terminated block is printed.

  * bin/: .lib.bat, libpath.bat:
	new libpath based on .bat-lib framework. (realpath/lapiota in future will base on this framework)

2007-08-23
  * bin/so.pl:
	subject to using Lisp-style script language.

  * bin/relay.pl, lib/cmt/serv.pm, lib/cmt/simpled.pm, t/st.pl:
	support of cmt::serv->new with -options

  * lib/cmt/util.pm:
	- get_named_args with nolead option to remove -(dash) leader
	- add select_input/seleci which redirects the STDIN

  * bin/resolv.pl:
	use only Socket::inet_ntoa

  * bin/pcs.pl:
	- framework by .net.pl
	- redirect STDIN/STDOUT to the socket handle
	- init script and help info are went into YAML data.

2007-08-22
  * bin/: pcs.bat, pcs.pl, pct.pl:
	new perl-console (server/terminal)

  * lib/cmt/util.pm:
	- qsplit now interpolate the "..." and '...' strings (maybe even a bit faster)
	- readfile/writefile in binmode

  * lib/cmt/test.pm:
	add cps()

  * lib/cmt/table.pm:
	add |-style table file parser

  * lib/cmt/oop.pm:
	- unified the lex/lexer/yylex name convention
	- add debug options to yparser->() returned by oop_newparser

  * lib/cmt/: oop_mod.pm, oop_y.pm, oop_y.y:
	add support of %lexer, %yylex attributes.
	add debug options to yparser->() returned by oop_newparser

  * lib/cmt/lexutil.pm:
	- unified the lex/lexer/yylex name convention
	- if the last token __xxx is returned after EOF, then the token is changed to EOF-token.
	- moved lex_dump from cmt::oop to cmt::lexutil

2007-08-18
  * lib/cmt/table.pm:
	first test passed. (with _cut2->_cut1 unreversable exception.)

  * lib/cmt/table.pm:
	initial for yaml/table format.

2007-08-16
  * lib/cmt/oop_mod.pm:
	can't shift @_ because $_[i] is in used in the semantic actions.

  * bin/source.bat:
	- accepts a filename as source
	- no setlocal, so environment changes are took affected. But when source.bat is used in pipe-mode, this doesn't work. (maybe the source.bat is executed in a separated process, so it can't modify the current process(shell)'s environment. )

  * bin/relay.pl:
	add --rawdump=charrange to suppress some unwanted chars(like \a beep) for reading purpose.

  * lib/cmt/oop_mod.pm:
	@_ includes "yylval" in semantic actions, so the first component is shifted out.

  * lib/cmt/: oop_y.pm, oop_y.y:
	- using more strict grammer, don't use empty=() any more.
	- also repeat-delimiter can't be empty.
	- carefully adjusted the precedence, so all conflicts are resolved as supposed.

  * lib/cmt/perlsys.pm:
	which_sub now supports to specify the sub by name.

  * lib/cmt/oop.pm:
	- catch 'die' from Yapp::Driver.
	- output diagnostic info only if any warnings.

  * lib/cmt/Makefile:
	using bash shell instead of cmd

  * bin/kopy.bat:
	add a DEST option to override default system file locations

2007-08-14
  * bin/syset.bat:
	add xt/bin to path

  * lib/cmt/: lexutil.pm, oop.pm, oop_mod.pm, oop_y.pm, oop_y.y:
	- implemented oop_compile and the final oop. congratulation!
	- syntax exp/alias ::= alias=exp
	- semantic {repeat} => plain-array
	- semantic concat[1] => scalar

  * lib/cmt/english.pm:
	add 'alternative when empty string' for simplify.

2007-08-13
  * lib/cmt/english.pm:
	add plural()

  * lib/cmt/: lexutil.pm, oop.pm, oop_mod.pm, oop_y.pm, oop_y.y:
	- add (empty), {n..m range}
	- implemented oop_conv which converts rw-def to yacc-def

2007-08-12
  * lib/cmt/: .class.pm, Makefile, oop.pm, oop_mod.pm, oop_y.pm, e.t.c...
	implemented oop_parse.

2007-08-11
  * bin/ccat.pl:
	add support of "expression" source files

2007-08-09
  * bin/: .plbat.lst, ccat.bat, fddump.bat, u.bat:
	add generator version

  * bin/plbat.pl:
	test ok

  * bin/plbat.pl:
	add var $GENERATOR

  * bin/plbat.pl:
	add --make for batch processing

  * bin/: ccat.bat, ccat.pl, fddump.bat, fddump.pl, source.bat:
	create for mp3 tags

  * bin/mp3ltrim.pl:
	change --at=N based on 0, and support --rem(remarks/removal) better

  * bin/.pl:
	help page quoted in single-quote(')

  * bin/.bat:
	add prep3

2007-08-06
  * bin/mp3ltrim.pl:
	enhanced support of delimit by space

2007-08-04
  * bin/.bat:
	more unix-like help info.

  * bin/cpnew.bat:
	adopted .bat-framework

2007-08-03
  * bin/: .cvsignore, .plbat, .plbat.lst, plbat.pl:
	new pl/bat wrapping method

  * bin/unchm.bat:
	replaced by unchm.pl

  * bin/.cvsignore:
	the .bat filelist in .cvsignore will be used to do the batch update of perl/bat-wrappers.

  * bin/.plbat:
	don't bind .bat wrapper with .pl anymore, for compat with unix.

  * bin/.batch.pl:
	see fswalk.

  * bin/mkchm.pl:
	initial

  * bin/unchm.pl:
	first release

  * bin/syset.bat:
	add .chm/Uncompress
	add PAGER=less

  * bin/repl.pl:
	adopted fswalk.

  * bin/nfs.pl:
	auto_mkdir -> mkdir_p

  * bin/relay.pl:
	no use Socket

  * bin/lstconv.pl:
	remove grouped roots from root list.

  * lib/fun/: .pm, _booter.pm, asknet.pm:
	append fun's help page to the funpack's help page

  * lib/cmt/util.pm:
	- _or is preferred to ||, which using defined() rather then true()
	- array_index/hash_index default using eq instead of ==
	- add de-alias to get_named_args.
	- added listdir() and fswalk()

  * lib/cmt/pp.pm:
	add typecheck to ppdom(&;FILELIST) where FILE could be a GLOB or ARRAY-REF.

  * lib/cmt/path.pm:
	+ mkdir_p, ishidden

  * lib/cmt/chm.pm:
	impl chm_hhk, chm_build, chm_compile

  * lib/cmt/.pm:
	use vars -> our.

  * bin/dsd.pl:
	rename tsh -> dsd; fixed 'h'-hide no affects.

  * bin/dsl.bat:
	rename tin.bat -> dsl.bat

2007-07-31
  * lib/cmt/path.pm:
	temp_name generate with process-id, so as to avoid name conflicts.

  * bin/asknet.pl, bin/domp.pl, lib/fun/asknet.pm:
	implemented asknet/where to query location info about a mobilephone number.

  * bin/catname.x:
	ppvar -> ppvarf

  * bin/.pl:
	new help page format (more unix-like)

  * bin/fun.pl:
	the branching part went into fun::_booter.

  * lib/cmt/netutil.pm:
	add post(url, post-data) and loadurl(url, [post-data])

  * lib/cmt/pp.pm:
	change ppvar to ppvarf, and the old ppvar became to accept %hash instead of &callback.

  * lib/cmt/util.pm:
	add _use() which evaluates 'require+import' at later time.

  * lib/fun/: .pm, _booter.pm, test.pm:
	splitted _booter from common fun-programs

2007-07-27
  * lib/cmt/chm.pm:
	initial

  * lib/cmt/atext.pm:
	new abstract-text model for dynamic texts.

  * lib/cmt/: netutil.pm, util.pm:
	- keep the -(dash) in option names.
	- moved network-related functions to netutil.pm

  * lib/cmt/: inet.pm, ios.pm, serv.pm, stream.pm:
	keep the -(dash) in option names.

  * lib/cmt/pp.pm:
	- add -options: -qdef, -qset, -pdef, -pset to select the tokens; -rem to remove the quote-char self.
	- add ppdom to support simple structured text.
	- new ppfmt_foobar to replace ppvar which support more complex grammer.

  * bin/syset.bat:
	change.plc to .pc

  * bin/Makefile:
	compile *.pl to *.pc

  * bin/hex.bat:
	back into hex.pl

  * bin/hex.pl:
	- make a unicode-grep by \x?? work-arounds.
	- updated help info

  * bin/hex.bat:
	formatted the help info

2007-07-26
  * bin/.batch.pl, lib/cmt/pp.pm, lib/cmt/util.pm, lib/fun/xs.pm:
	add file-handle support in cmt::pp, also the pp() function now accepts token-config (you can customize which of /*, # etc. comment chars are used)

2007-07-23
  * bin/chmac.pl, lib/Net/Adapter.pm, e.t.c...
	add select by combo option (the index is changed after netcon_reset, however the combo seems to be static.)

  * bin/cvsuser.bat:
	create the cvsuser utility to replace the pserver+ project.

  * bin/syset.bat:
	by insert _HEAD_ placeholder into PATH variable, to make some path always at the very beginning.

  * bin/sh.bat:
	take account of USERNAME, SHELL, and auto 'mount'ing the root fs (by mapping B:).

  * bin/perldev.bat:
	clear $SHELL, which is used in make program.

  * bin/chmac.pl:
	initial released

  * bin/: hexdump.pl, relay.pl:
	impl hexdump

  * bin/loop.bat:
	add support of statement-list

2007-07-22
  * lib/cmt/util.pm:
	add array_index, array_remove, format_inaddr, sockinfo

  * bin/relay.pl, lib/cmt/ios.pm, lib/cmt/serv.pm, e.t.c...
	relay initial released!

  * lib/cmt/inet.pm:
	call tcp_connect with (... -cont=>1) to leave the cmt::ios::context running.

  * bin/: myexec.c, myexec.exe, perldev.bat, syset.bat:
	add extension of .sh unix shells, and exec bash by myexec to resolve the slash-problem.

2007-07-21
  * bin/: .bat, moduse.bat:
	new .bat template provide common structure and to resolve the "%* shift bug"

  * bin/lstconv.pl:
	add support of virtual-group: "[Group] path"

  * lib/cmt/util.pm:
	atexit -> at_exit to avoid name conflicts

  * lib/cmt/ios.pm:
	new methods in cmt::ios::context: add, count, exists, remove

2007-07-20
  * lib/cmt/ipc.pm:
	deprecated, using cmt::ios instead.

  * lib/cmt/channel.pm:
	deprecated, using cmt::stream instead.

  * lib/cmt/util.pm:
	add fire_sub to invoke simple-callback.

  * lib/cmt/stream.pm:
	change syntax from stream->($fd, @data) to stream->write($data, [$fd])

  * lib/cmt/serv.pm:
	add create_ios method to create the serv/ios only for custom extensions.

  * lib/cmt/ios.pm:
	add cmt::ios::merged derivation:  when multiple cmt::ios were created, they have to be 'loop'ing in separated threads, this cause many problems.  the new merged-model allows one to 'loop'ing in one thread, by IO::Select all the sockets of cmt::ios(s).

2007-07-18
  * bin/relay.pl:
	adapted cmt::serv & cmt::inet

  * lib/cmt/ios.pm:
	default reads/writes/errs group names as "READ"/"WRITE"/"ERR"

  * lib/cmt/inet.pm, lib/cmt/stream.pm, t/ct.pl:
	- tcp_connect with additional paramters passed to new IO::Socket::INET.
	- unified handles in cmt::stream->{IO}, add hinfo debug property.
	- add more debug outputs

  * bin/words.txt:
	-> words.txt.gz

  * bin/whoare.pl, bin/words.txt.gz, lib/cmt/util.pm:
	whoare now accepts words file in .gz format.

  * bin/: dnac.pl, dnad.pl:
	back to decoded version.

  * lib/cmt/util.pm:
	add setnonblock/nbread

  * lib/cmt/inet.pm, lib/cmt/ios.pm, lib/cmt/serv.pm, e.t.c...
	- new cmt:;stream (rewrite of cmt::channel, which is obsoleted).
	- convention: cmt::stream->{ctx} refer to cmt::context object.
	- add cmt::ios -init callback.
	- replaced IO::Select by cmt::ios in cmt::serv.
	- rename t-client, t-serv to ct/st.

  * lib/cmt/.class.pm:
	add info/info2 in the class-module template.

  * lib/cmt/channel.pm:
	deprecated, using cmt::stream instead.

2007-07-17
  * bin/execgui.pl, bin/odx.pl, lib/cmt/guiutil.pm, lib/cmt/ios.pm:
	new cmt::ios to simplify the usage of IO::Select.
	refactor guiutil to use cmt::ios. (seems a lot more faster!)
	(and no more "refresh, interval, timeout, etc. " ambiguous keywords, just "auto" only.)

  * lib/cmt/util.pm:
	add get_named_args

  * lib/cmt/ftime.pm:
	using prototype() to avoid the function absorb arguments.

2007-07-16
  * lib/cmt/channel.pm:
	derive from cmt::channel if you want to implement your "recvf".

  * lib/cmt/serv.pm:
	fire_init when connection established.

  * bin/runx.pl, lib/cmt/util.pm:
	add cmt::util::atexit, don't use eval('END {}') any more.

  * bin/nfs.pl:
	add examples in help page

  * bin/: .pl, cpnew.bat:
	cpnew to do files-update, with hidden-files, readonly-overwrite support, etc.

  * bin/syset.bat:
	- add more console (cmd.exe) preferences
	- output in more detail

  * abc.d/bin/sml.bat, bin/big.bat:
	using 'start "big"' to have a big-font window

  * bin/syset.bat:
	perl, python's blib -> site/lib

2007-07-15
  * lib/fun/xs.pm:
	using strncpy instead of memcpy for a string-type

  * lib/cmt/util.pm:
	+ indent/unindent_most

  * bin/mani.bat:
	change default list file from "MANIFEST" to ".MANIFEST"

  * bin/syset.bat:
	add include/lib path

  * bin/: hex.bat, ugrep.bat:
	make a unicode-grep by \x?? work-arounds.

  * lib/fun/xs.pm:
	initial release

  * lib/fun/xs.pm:
	don't dump typedef *PTRTYPE.
	add --struct-only=<structs...>

  * bin/fun.pl:
	error-continue when autoload.

  * bin/: lstconv.pl, mani.bat:
	always add MANIFEST file to the list self.
	introduced the extern/ for the absolute paths.

  * bin/catname.x, lib/cmt/proxy.pm, lib/cmt/util.pm:
	resolved the (local $_) can't be set problem, by "localuntie $_" facility. (see cmt::proxy also)

2007-07-14
  * lib/cmt/pp.pm:
	moved Roco::Misc/interp_params -> cmt::pp/ppvar

  * lib/cmt/util.pm:
	forx: changed order of code/regexp -> regexp/code

  * bin/foreach.pl:
	add support of user options, so in x-mode you can specify "#?foreach -o myopt ... "
	add dmp built-in function
	make the variables related, so change one variable will cause another to change. (using cmt::cstr)

2007-07-13
  * lib/: cmt/pp.pm, fun/xs.pm:
	stwrap impl.

  * bin/fun.pl:
	- autoload once
	- add support to cleanup by pkg->exit

  * lib/: cmt/.class.pm, cmt/.pm, cmt/win32.pm, fun/.pm:
	my $opt_xxx -> our

2007-07-12
  * bin/: kopy.bat, ppid.c, ppid.exe, syset.bat:
	rewrite kopy using wfpreplace, and replace cmd.exe by spawn a new console, with waiting for ppid.

  * bin/perldev.bat:
	add psdk/include, psdk/lib

2007-07-11
  * bin/loop2.pl:
	exec... -> win32::spawn
	killpid -> win32::ntkill

  * bin/dsd.pl, bin/dsl.bat, bin/waitpid.c, bin/waitpid.exe, e.t.c...
	waitpid with a timeout

  * lib/fun/xs.pm:
	add xs/~stwrap

  * lib/fun/test.pm:
	add ~API, for calling SFC_OS::TerminateSFC.. (undocumented)

  * lib/fun/.pm:
	fun/~sub template enhancement

  * lib/cmt/util.pm:
	bsearch -> bserchi to avoid POSIX::bsearch

  * lib/cmt/perlsys.pm:
	ctrl-d to terminiate the stdin

2007-07-10
  * bin/fun.pl, lib/fun/.pm, lib/fun/i18n.pm, lib/fun/test.pm:
	- using stash to get package of a subroutine
	- autoload fun/modules

  * bin/psh.pl, lib/cmt/perlsys.pm:
	add a simple perl shell

  * lib/cmt/util.pm:
	moved hashindex(find_key), hash2tuples, bsearch (Roco::Misc --> cmt::util)

2007-07-07
  * bin/lstconv.pl:
	Subdirectories goes into Groups in the ue-project.

  * bin/killtemp.bat:
	'killtemp name*' => 'killtemp - name'

2007-07-06
  * bin/killtemp.bat:
	call 'killtemp hide*' to show/restore the hidden window (of loop2)

  * bin/: mani.bat, syset.bat:
	better system integration, MANIFEST.lst -> MANIFEST.prj (instead of .lst.prj)

2007-07-05
  * lib/: auto/cmt/low/.packlist, auto/cmt/low/low.dll, cmt/low.pm:
	make by "makefile.pl PREFIX=%DIRT_HOME%/0"

  * lib/: auto/cmt/low/.packlist, auto/cmt/low/low.bs, e.t.c...
	the first xsub.

  * bin/loop2.pl:
	added hide/show feature

  * bin/loop2.pl:
	implement 'restart' by 'touch', and changes the event-queue styles.

  * bin/readcode.pl:
	gui enhancement

  * bin/loop2.pl:
	keypress 'r' to restart

2007-07-04
  * bin/mani.bat:
	using 2-level iteration: "for /r /d" because "for /r" scans hidden files

  * bin/mani.bat:
	add option -r to regenerate .prj file .

  * bin/mani.bat:
	add option -r to regenerate .prj file .

  * bin/filetime.bat:
	(fixed) return 0 if file not exists. (so to test "file A is newer then B")

  * bin/syset.bat:
	don't reset metapad's color

  * bin/foreach.pl:
	- globalized temp var $a..$z.
	- fixed a mistake in r = ren()

2007-07-02
  * bin/: perldev.bat, syset.bat:
	changed some path

  * bin/: lstconv.pl, mani.bat:
	generate ultraedit project file easily.

  * bin/foreach.pl:
	--files-only or -f; better help text

  * bin/filetime.bat:
	return 0 if file not exists. (so to test "file A is newer then B")

  * bin/foreach.pl, t/foreach-1.x:
	rewrite using .pl template. (a changement is lost acidently.)

2007-07-01
  * bin/perldev.bat:
	win32 %RANDOM% is timer seeded, so dropped the pseduo seed to get a better random.

  * bin/: perldev.bat, syset.bat:
	using environment %PERL% = path\perl.exe, to support alternative versions of perl.

  * bin/syset.bat:
	assoc .x=runx

  * bin/runx.pl:
	new .x file type for shell-script

  * bin/sev.pl:
	code formatted using .pl

  * bin/redperl.c:
	add p=pause, q=quote options

2007-06-30
  * bin/: spawn.c, spawn.exe:
	Win32 doesn't support fork well: save PID value to file named $ENV{'pid_file'}.

  * bin/loop2.pl:
	add -scriptdev option

  * lib/cmt/codec.pm:
	add %CODECS for readcode.

  * bin/hackdict.pl:
	using better binhex/hexbin in codec.pm

  * bin/.pl:
	exit code correctly

  * bin/autoyes.bat:
	loop.bat change unit ms to sec

  * bin/loop.bat:
	deprecated.

2007-06-25
  * bin/clup.bat:
	- custom clup by config .clflags file
	- add bundled clup:util

  * bin/apply.pl, bin/c-text.pl, bin/cleanhtml.pl, bin/clip.pl, e.t.c...
	replace #!perl -> #!/usr/bin/perl

  * bin/repl.pl:
	bugfix

  * lib/cmt/util.pm, t/t-qsplit.pl:
	add forx regexp processor
	add qsplit to support append_cmdline

  * bin/foreach.pl:
	define lexical sub as sub f "; *f = sub" {...}

2007-06-23
  * bin/repl.pl:
	.pl-template applied

  * bin/rmclear.bat:
	bugfix

2007-06-20
  * bin/mp3ltrim.pl:
	template applied, and add -1, -2, etc. part removal support.

  * bin/foreach.pl:
	rename style as "new=f(old); r; "

  * bin/.pl:
	text_with_underline for easy selection.

  * bin/in.pl:
	modify alias if exists; and set alias="" to remove it.

2007-06-15
  * bin/repl.pl:
	code style

  * bin/foreach.pl:
	add --sort (name,size), --reversed sorting features
	add inline-script (or @script-file)
	add file-list reference options (--list=..., and $L, $M, etc. vars)

2007-05-23
  * bin/.batch.pl, bin/tabsize.pl, bin/tabsizes.bat, lib/cmt/path.pm, e.t.c...
	rewrite tabsize.pl, and add a new template .batch.pl to make life easier.

2007-05-12
  * bin/: execj.bat, spawn.c, spawn.exe, waitpid.c, waitpid.exe:
	execj initial released.

  * bin/hackdict.pl:
	fixbug: stop when read '0'.

2007-05-01
  * bin/hackdict.pl:
	profiler.

2007-04-30
  * bin/wm.pl:
	add \{del}, \{show}, \{hide} ctrls

  * abc.d/bin/em.bat:
	hide console

  * bin/hackdict.pl, t/animals, t/numbers, t/passwd, t/verbs:
	dev.

2007-04-27
  * lib/cmt/winuser.pm:
	add %_ID, %_WM, %_VK constants

  * lib/cmt/path.pm:
	bugfix: to distinguish if ('') and if(undef)

  * bin/xvconv.bat:
	automaticly copy mencoder.exe to mc.exe (this change the display-name in tasklist. for hacking purpose)

  * bin/tup.bat:
	add xdel to TUP for cleaning unnecessaries parts

  * bin/rundelay.pl:
	dev.

  * bin/repl.pl:
	linux compat.

  * bin/nu.bat:
	add -v (verbose) option to nu.bat

  * bin/loop.bat:
	add delay option to loop.bat

  * bin/libstr.bat:
	add uc, lc for case convert

  * bin/discuz.pl:
	disable Tk-GUI temporary

  * bin/wm.pl:
	add send-string, send-input

  * lib/cmt/path.pm:
	add temp_home, temp_path

  * bin/xvconv.bat:
	fix mistake (%_rotate% -> 1)

  * bin/nfs.pl:
	add --test-only for retrieve nfs-home

  * bin/foreach.pl:
	new safe ren() method and --test-only option

  * bin/dcall.bat:
	variation dCall, DCALL for different verbose modes

2007-03-17
  * bin/xvconv.bat:
	goto end after [* batch] finished

  * bin/xvconv.bat:
	customize bitrate: '[' <number> ']'

  * bin/xvconv.bat:
	support more custom options

  * bin/xvconv.bat:
	override default extension to .avi

  * bin/pc.pl:
	print-counter now supports input-echo (by \r only)

2007-03-04
  * bin/jads.bat:
	rewrite

  * bin/syset.bat:
	add "send to sf..." command

  * bin/: recv.bat, send.bat:
	"start cmd" if run in windows, or "exec cmd" if run in console

  * bin/syset.bat:
	add mp3ltrim to mouse-right-menu

  * lib/cmt/util.pm:
	delete "my"

2006-12-24
  * lib/cmt/util.pm:
	del duplicated "my"

2006-12-23
  * bin/nfs.pl:
	(unresolved) want to change directory to NFS_Container when specify option -to

  * lib/cmt/util.pm:
	changed cftime to using gmtime instead of localtime.
	add localftime.

  * bin/names.pl:
	modified for mp3 tagnames

  * bin/dcall.pl:
	rewrite dcall.bat

  * lib/cmt/util.pm:
	fixed a calc error in millisecond

  * bin/dcall.bat:
	replaced by new dcall.pl

2006-12-13
  * bin/nfs.pl, lib/cmt/path.pm, lib/cmt/util.pm:
	add support of dynamic symbolic link (<?...?>)

2006-12-12
  * bin/fun.pl, lib/fun/.pm, lib/fun/i18n.pm, lib/fun/test.pm:
	fixed returns values

  * bin/fun.pl, lib/fun/i18n.pm:
	new i18n (~is_utf8) for nfs/six refactor

  * bin/fun.pl, lib/fun/.pm, lib/fun/test.pm:
	new fun.pl to run utilities functions as programs

  * bin/nfs.pl:
	use cmt::date to support datetime function

  * bin/.pl, bin/fun.pl, lib/fun/test.pm:
	new fun.pl to run utilities functions as programs

2006-12-05
  * lib/cmt/path.pm:
	add dir_size

2006-11-18
  * bin/resolv.pl:
	modified version info

  * bin/sev.pl, t/sev-1.x:
	special form: %EVAL

  * bin/syset.bat:
	add font color.

  * bin/advname.bat, bin/apply.pl, bin/c-text.pl, bin/cleanhtml.pl, e.t.c...
	replace #!/usr/bin/perl -> #!perl

2006-11-16
  * bin/elaps.pl:
	elaps time test

  * lib/cmt/util.pm:
	ctime + ftime => cftime

  * lib/cmt/serv.pm:
	enable check against the deadloop

2006-10-21
  * bin/served.pl:
	the explicit name can't be guessed...

  * bin/syset.bat:
	changed metapad settings

2006-10-20
  * bin/sim.pl, lib/cmt/util.pm:
	sub scanDirectory is merged to sim.pl

  * bin/keeplink.pld, lib/cmt/serv.pm:
	add log to file support, with date column added.

  * bin/syset.bat:
	modified metapad settings
	readd cygwin disk (B:)

  * lib/cmt/service.pm:
	fixed the missing of 'restart' command

2006-10-19
  * bin/syset.bat:
	add .pld, .six extensions

  * bin/served.pl:
	initial.

  * bin/wcc.pl:
	add .pm files

2006-10-18
  * bin/keeplink.pld:
	append \n to the send-text. otherwise the sending is buffered.
	adjust verbose info.

  * bin/keeplink.pld:
	using simpled/echod as a dummy server

  * bin/simpled.pld, lib/cmt/channel.pm, lib/cmt/ftime.pm, e.t.c...
	new cmt::serv help creation of services.
	new cmt::simpled implemented simple tcp services.

2006-10-17
  * bin/rfc.bat:
	only assoc metapad to text type file.

  * etc/service/echohh.php, lib/cmt/echohh.php:
	move echohh.php from lib/cmt/ to service/

  * bin/syset.bat:
	add ARK_HOME var.

  * bin/rfc.bat:
	add ARK_HOME environment variable to replace z:/Files/ directory.
	set default editor to metapad.

2006-09-15
  * bin/usecode.pl:
	change registry path /S-FIA../Dir-T/ to /DIRT/

  * bin/syset.bat:
	add JAVA_LIB env

2006-08-19
  * bin/nfs.pl:
	set defaulat home note/ to .note/

  * bin/: listen.bat, open.bat:
	override PORT

  * bin/syset.bat:
	std env: dirt_home, cirk_home

2006-07-27
  * abc.d/bin/sml.bat, bin/apply2.bat, bin/clup.bat, bin/jads.bat, e.t.c...
	initial

  * bin/whoare.pl:
	print atom in multi-thread mode.(which is safer)
	rather print fragments in singlethread mode (which is faster)

  * bin/wcc.pl:
	add bas file type

  * bin/tup.bat:
	no more use q.host.bodz.net

  * bin/: cvsco.pl, cvscosf.bat:
	sf cvs web changed.

  * bin/apply.pl:
	using cmt::path

  * bin/getweb.bat:
	add header: Host: <host>

  * bin/cvsignor.def:
	ignore *.patch, *.lnk

  * bin/rfc.bat:
	add vars

  * bin/syset.bat:
	using cygwin disk (B:)

  * bin/cvsignor.def:
	ignore _win32_ ...

2006-04-23
  * bin/syset.bat:
	add merge command to *.patch fiels

  * bin/wcc.pl:
	add .sql type

  * bin/tabsizes.bat:
	display help if no args

  * bin/rfc.bat:
	path changes

  * bin/dist-cvsi.bat:
	quote "%TEMP%" if temp dir contains space

  * bin/cvsignor.def:
	ignore *.lnk and *.patch

  * bin/nfs.pl:
	add test-only option

  * bin/cvscopy.bat:
	add cvscmdopts so can copy specified tag/branch

2006-03-17
  * lib/cmt/winuser.pm:
	code review

  * bin/: cvsignor.def, dist-cvsi.bat:
	new solution

  * bin/examples:
	examples file

  * bin/wcc.pl:
	ctl => vb

  * bin/nfs.pl:
	retry if link not available

  * bin/cvscopy.bat:
	writable checkout

2006-01-26
  * bin/syset.bat:
	modulize, add include/lib to .cirkonstancoj/sdk

  * bin/srt-delay.pl:
	add scale function

2005-12-20
  * bin/nfs.pl:
	add "directory of any files" note type, this replace the old work-box.
	the symbolic now must with extension .link

  * bin/mp3ltrim.pl:
	fix filename pattern

  * bin/winvod.pl:
	customizable server

  * bin/wcc.pl:
	add xml extension (but not impl.)

2005-10-28
  * bin/deltags.bat:
	initial

2005-10-21
  * bin/repl.pl:
	die if file error

  * bin/syset.bat, abc.d/bin/plts.bat:
	maintaince

  * bin/copyfix.pl:
	initial

  * bin/: forx.pl, foreach.pl:
	add userproc variables

  * bin/cvscopy.bat:
	fixed complex module path bug.

2005-07-17
  * bin/forx.pl:
	fix recursive bug

  * bin/foreach.pl:
	prompt for enter script

  * bin/forx.pl:
	initial

  * bin/foreach.pl:
	add ls, search from forx.pl, fix $count undefined.

2005-07-14
  * bin/cvscopy.bat:
	fix sync-.bat filename bug

  * bin/re.bat, abc.d/bin/plts.bat:
	for ruby-scite & mxscheme

  * bin/: syset.bat, tup.bat:
	add ruby component

  * bin/foreach.pl:
	fixed recursion bug

  * bin/cvscopy.bat:
	syntax changed. not compatible to lower than 1.7

2005-07-08
  * bin/syset.bat:
	include metapad

2005-07-06
  * bin/: discuz.pl, simxml.pl:
	change --picture to --icon (ic)

  * bin/winvod.pl:
	re-add (no dna-encoding)

  * bin/discuz.pl:
	fix --cookie/--caption ambigouous
	add --picture

2005-07-05
  * bin/discuz.pl:
	fix arguments, add

2005-07-01
  * bin/dnac.pl, bin/dnad.pl, lib/cmt/g.pm:
	full-DNA

  * bin/dnac.pl, bin/dnad.pl, lib/cmt/g.pm:
	test ok

2005-06-30
  * bin/: syset.bat, tup.bat:
	.dir-t -> .dirt

2005-06-19
  * bin/cvscopy.bat:
	attris -> attrib

  * bin/cvscopy.bat:
	make readonly copy

  * bin/cvscopy.bat:
	don't clean, for merge

2005-03-30
  * bin/noteauto.vbs:
	add comments about .vbs-association
	this is always be modified by sthvcd

2005-03-03
  * bin/: dnac.pl, dnad.pl:
	Compiled by DNA-Compiler

  * bin/dnac.pl:
	indent mistake

  * bin/dnac.pl, bin/dnad.pl, lib/cmt/g.pm, lib/cmt/util.pm, e.t.c...
	initial

2005-03-02
  * bin/cuelist.pl:
	initial

2005-01-19
  * bin/hex.pl:
	add ccr (control-character representation) support

  * bin/nfs.pl:
	generate .nfs-sync.bat with --quiet option

  * bin/hex.pl:
	add chars support, e.g.:
	  hex -p=$dh. 'hello'
	  hex -p=$dd. '.upd'

2005-01-06
  * bin/cvscopy.bat:
	fm.  "Revision"

  * bin/cvsapp.pl, bin/cvsco.pl, bin/dirs.pl, bin/hex.pl, bin/in.pl, e.t.c...
	fd.  cmt.pm => cmt/vcs.pm

  * bin/nfs.pl:
	- add generated bat script
	- add interactive option
	- enhanced user interface

2005-01-05
  * bin/cvscopy.bat:
	don't create .vex directory cuz not used.

  * bin/cvscopy.bat:
	in.

2005-01-04
  * bin/nfs.pl:
	fd- cmt.pm => cmt/vcs.pm

  * bin/note.bat:
	Nfs-Transition

  * bin/nfs.pl:
	use . for today and .. for yesterday

  * bin/nfs.pl:
	more convienient offset format, like days<-.., ..->days

  * bin/nfs.pl:
	implement offset option

  * bin/nfs.pl:
	fb- entry in sync-nfs is already expanded.

  * bin/nfs.pl:
	add remove mapping-entry support

  * bin/nfs.pl:
	fb- map support

  * bin/nfs.pl:
	auto-extension

  * bin/nfs.pl:
	initial

  * bin/fontmap.php:
	add more powerful char-table format ( \x?? char support )

  * bin/nfs.bat:
	shell wrapper

  * bin/syset.bat:
	- fixed PERLLIB/PYTHONLIB incorrects
	- using normalized-path instead of mixed-slash path

  * bin/: scx.pl, tgz.bat, nfs.pl:
	initial

  * lib/cmt/path.pm:
	pa. some comments

  * lib/cmt/winuser.pm:
	initial

  * lib/cmt/test.pm:
	mi. from webcon::Test::Base

2005-01-03
  * bin/nfs.pl:
	rewritten of note.bat, initial

  * lib/cmt/path.pm:
	mi. from webcon

2004-12-22
  * bin/j.bat:
	adjust classpath

  * bin/note.bat:
	add link maintaince

2004-12-19
  * bin/: note.bat, noteauto.vbs:
	add multi .vol/ support

2004-12-12
  * bin/in.pl:
	initial

  * bin/hex.pl:
	fixed a display char mistake

  * bin/newline.pl:
	- add global filename-pattern support
	- add wild-chars in filename support

  * bin/repl.pl:
	missing of "use cmt"

  * bin/repl.pl:
	split help into version and help,
	(cuz repl.pl was refered too much, its code will be using as template)

2004-12-07
  * bin/notes.zip:
	move .help out from .def

  * bin/note.bat:
	fixed a reentrant bug

  * bin/so.pl:
	enhanced escape/unescape functionality (see re-parts reference impl.)

  * bin/re-parts.pl:
	- show more detail of position info
	- add reference implementation of complex global replace

  * bin/newline.pl:
	display current line-format info in help page

  * bin/note.bat:
	- option // now build index file in given container-name, instead of temp dir
	- symbolic link filename of note-home now use Notes.link instead Notes
	- resolve .name to .vol-name

2004-12-03
  * bin/hex.pl:
	initial

  * bin/fontmap.php:
	removed 7f-9f from default chars

  * bin/syset.bat:
	- change php-upload/session directory to userhome/local settings/
	- include php.ini template in t/2/etc/

  * bin/so.pl:
	fixed: replace escaped-char as \n \r \t \\  seperately, instead of \(.)=>table-lookup

  * bin/rfc.bat:
	rename uedit32 to ue

  * bin/note.bat:
	- add /<open-app> options at end of command-line
	  (as: note [{//|/del|/<ext>}] {<empty>|.|<name>} [/<open-app>]
	- support '.' for auto-serial naming (also empty string does)

  * bin/so.pl:
	add --print, --wait options
	rename options with opt_ prefix

2004-12-01
  * bin/: note.bat, syset.bat:
	uedit32 renamed to ue

  * bin/syset.bat:
	fixed an unescaped-% error

  * bin/fontmap.php:
	- use Courier New as default text font
	- enhanced angle-rotate option, but still have bug (when angle >45, crop happened.)
	- implemented --bold option by repeat drawing along x-axis.

  * bin/fontmap.php:
	- add interlace option for png-interlace or jpeg-progressive
	- index the options by alphabet-order
	- add text-format for build character cartoons,
	- add --textfont options to view the cartoons in different fonts.

  * bin/fontmap.php:
	- add format-dependent codes
	- add jpeg-quality option

2004-11-30
  * bin/syset.bat:
	add php configuration

  * bin/syset_env.exe:
	rewritten in perl script embedded in syset.bat.

  * bin/args.bat, t/libstr_t.bat:
	initial

  * bin/note.bat:
	moved replace/join into libstr, and modified the name.

  * bin/syset.bat:
	- implemented environment-updater in perl instead of calling syset.exe, so full-script.
	- enhanced dir-t path layout
	- add php in dir-t.

  * bin/libstr.bat:
	Code clearify

  * bin/libstr.bat:
	use revision instead of id in function comments

  * bin/libstr.bat:
	- initial

  * bin/fontmap.php:
	- fixed a version display error
	- show help when family not specified

2004-11-29
  * bin/fontmap.php:
	add sameh option

  * bin/fontmap.php:
	- have calculated the correct formula of Y-positioning to chars
	- fixed a auto [tsnlh] naming error
	- fixed a gradient color error in anti-alias

  * bin/fontmap.php:
	Initial

2004-11-28
  * bin/note.bat:
	Fixed bug: when /<ext> specified, the new note is always overwrite the old.

2004-11-25
  * bin/j.bat:
	JOS Home Moved

  * bin/dirs.pl:
	[DIRS] Like old "T" utility for calc directories' size
	- Initial

  * bin/note.bat:
	Add option // for list titles of all notes.

2004-11-24
  * bin/j.bat:
	New J-shell

  * bin/: note.bat, noteauto.vbs, notes.zip:
	add template note-support in self, using "note .vol-def"

2004-11-17
  * bin/newline.pl:
	fix an CR-redundancy when output lines by use binmode()

  * bin/newline.pl:
	add binary-file recognize.

  * bin/newline.pl:
	Initial-2

  * bin/newline.pl:
	Initial

  * bin/sim.pl:
	Add temp extension-name support for .bat, .vbs using.

2004-11-15
  * bin/syset.bat:
	add .sim assoc

2004-11-13
  * bin/whoare.pl:
	- add lot more whois server list.
	- iterate by word-list file, and resume support.
	- iterate by numbers only.
	- enhanced multi-threads function.
	- general name query (include .com, .net, .cn etc.)
	- display each record part by part, by the stages of query session.
	- new log file naming method.

  * bin/: minimode.bat, monitor.pl:
	Initial

  * bin/resplit.pl:
	Regular-Expression SPLIT: Split into files by occurance of pattern-match
	- Initial

  * bin/rfc.bat:
	use ultraedit as the default editor.

  * bin/repl.pl:
	support output only mode (don't save/writeback the replace result)

  * bin/loop.bat:
	display command line at titlebar.

  * bin/note.bat:
	add /del option for delete note.

2004-10-26
  * bin/loop.bat:
	initial

  * bin/whoare.pl:
	- whois .biz server
	- auto naming log-file for fuzzy-query
	- debug/test options
	- sub whois/ fix un-reentrantable
	- remove (fhout) whois dump
	- more specific whois format support
	- resume fuzzy-query thru 'tail -1 logfile'
	- general name query (name.*)

2004-10-25
  * bin/whoare.pl:
	supp cnnic whois format

  * bin/whoare.pl:
	initial [whois *]

2004-10-20
  * bin/simxml.pl:
	[keep] this state-machine version is hard to maintain.

  * bin/sim.pl:
	Use temporary file for both write-mode and output-mode, to clear sim-preprocessors for specific interpreters.

  * bin/: sim.pl, simxml.pl:
	- Initial, SIM is A GG-style interpreter
	- Update .stx to .xml.sim

  * bin/note.bat:
	fixed a dirty-variable bug.

  * bin/cvscosf.bat:
	Splited into cvsco

  * bin/cvsco.pl:
	Generalized from cvscosf

2004-10-11
  * bin/binwrap.bat:
	indent.

  * bin/envkey.bat:
	env-clear for general purpose.

  * bin/noteauto.vbs:
	ole-automation support for note.bat.

  * bin/note.bat:
	- auto-recognize file type only if /type arguments not specified.
	- add ole-auto support for creating template-based documents.
	  (for avoid timestamp-bug in copy-mode)

  * bin/note.bat:
	- new extension mapping
	  (now note/xls .. instead of note e ..)
	- auto extension recognize
	  (now note <xlsnote> instead of note e <xlsnote>)

  * bin/cvscosf.bat:
	initial

  * bin/: binwrap.bat, cmdex.bat, envkey.bat:
	binwrap  [ testing Plink arguments ]
	   envkey
	      cmdex

2004-10-08
  * bin/cvsapp.pl:
	progress.

  * bin/note.bat:
	add link-following support, so notes package can be placed at different places.

  * bin/rfc.bat:
	initial

2004-10-02
  * bin/note.bat:
	add mount-check.

2004-09-24
  * bin/wcc.pl:
	- add total lines at last (forbidden by --nosummary)
	- add classification by .extension
	- don't show directories with zero lines, unless verbose-mode.

  * lib/cmt/vcs.pm:
	the literal $.I.d.$ is modified by cvs mistake, so fix it.

  * bin/cvsexp.bat:
	fixed a version display bug

  * lib/cmt/vcs.pm, bin/cvsapp.pl:
	initial

2004-09-23
  * bin/wcc.pl:
	use parse_id to show version info

  * bin/cvsexp.bat:
	add version info

  * bin/wcc.pl:
	fixed a sum bug

  * bin/wcc.pl:
	ignore Object .. at begining of vb files.

  * bin/wcc.pl:
	fix revision display

  * bin/wcc.pl:
	initial

  * bin/clip.bat:
	removed.

  * bin/repl.pl:
	- new option: --veryquiet
	- add to display modified-count at last.

  * bin/note.bat:
	- trailing '-' in 'title-' will be substituted by serial number

2004-09-22
  * bin/: HIS.BAT, LM.BAT, StartMap.bat, asp-decode.pl, jackhash.bat, e.t.c...
	these were expired or from 3rd-parties

  * bin/MCopy.bat, bin/MkRoot.bat, bin/SWAP.BAT, bin/TestErr.bat, e.t.c...
	add version info at header.

  * bin/repl.pl:
	few

  * bin/: frepls.bat, m.bat:
	removed

  * bin/: tabsize.pl, tabsizes.bat:
	source from tab-style.pl

  * bin/: c-text.pl, clear-recent.bat, cvsexp.bat, get-mp4cn.bat, e.t.c...
	initial

  * bin/tup.bat:
	auto initialize system settings after updated.

  * bin/stx2xml.pl:
	few

  * bin/so.pl:
	show help if no arguments are specified.

  * bin/repl.pl:
	add external command support

  * bin/getmed.pl:
	make help page more clear

2004-08-23
  * bin/tup.bat:
	at last, automatic update -- being commited!

  * abc.d/bin/em.bat, bin/dist-cvsi.bat, bin/j.bat, bin/srt-delay.pl, e.t.c...
	usnap merging plan

2004-08-08
  * bin/dist-cvsi.bat:
	Dispatch .cvsignore (and other initializers) to sub-directories

  * bin/HIS.BAT, bin/LM.BAT, bin/MCopy.bat, bin/MkRoot.bat, e.t.c...
	MERGED INTO USNAP PROJECT
