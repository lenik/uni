#!/usr/bin/perl
# $Id: clip,v 1.2 2004-09-22 08:39:08 dansei Exp $

use strict;
use Win32::Clipboard;

my $clip = Win32::Clipboard();
my @lines = ();

while (<>) {
	chomp;
	push @lines, $_;
}

$clip->Set(join("\r\n", @lines));

print "Total ".scalar(@lines)." lines. \n";

