#!/usr/bin/perl
# $Id: clip,v 1.4 2007-06-25 12:58:13 lenik Exp $

use strict;
use Win32::Clipboard;

my $clip = Win32::Clipboard();
my @lines = ();

while (<>) {
	chomp;
	push @lines, $_;
}

$clip->Set(join("\r\n", @lines));

print "Total ".scalar(@lines)." lines. \n";

