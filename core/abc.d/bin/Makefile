
# fucking...
#
# The SHELLFLAGS isn't supported, yet. may be added in future.
# There's no other way to override $(SHELL) -c

default:
	cmd /c jsub -Xx.bat -Ddelim=: -Dfile-field=name -Scsv -Mgsp=.autofind.bat.gsp .autofind.csv
