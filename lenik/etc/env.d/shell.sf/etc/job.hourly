#!/bin/bash

HDR=HOURLY

#WW Updates
	for project in xjl mego chsid msb freejava mp-3; do
		PDIR=/home/groups/${project:0:1}/${project:0:2}/$project/www
		if [ -d $PDIR ]; then
			echo $HDR WWW-Update $project
			cd $PDIR
			cvs update -d
		fi
	done
