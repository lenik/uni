
2007-07-15
  * .MANIFEST, .MANIFEST.prj, MANIFEST, e.t.c...
	first test passed.

  * netcon/: netcon.dsp, netcon.dsw, test.cpp:
	print the CLSID_ConnectionManager which doesn't exist in vs 6.0 sdk.

  * netcon/netcon.cpp:
	using local __uuidof to give CLSID_ConnectionManager.
	trim {} in given id

  * Adapter.xs, MANIFEST, Makefile.PL, README, ppport.h, typemap, e.t.c...
	initial
