2010-12-13  lenik

	* Makefile.am: include `allin.am` instead of setup rules for each
	  makefile.

	  using `do_subst += ...` to append more params.
	* configure.ac: Add .rst implicit rules and set default
	  `-Wno-portability` in `configure.ac`

2010-11-28  lenik

	* VERSION.av, configure.ac, debian/control, debian/control.m4,
	  debian/winetricks.install, debian/winetricks.postinst,
	  debian/winetricks.prerm: don't package winetricks packages any
	  more. they must be install by each user.
	* VERSION.av, configure.ac, debian/Makefile, debian/copyright,
	  debian/postinst, debian/prerm, debian/winetricks.install,
	  debian/winetricks.postinst, debian/winetricks.prerm: fix
	  postinst/prerm/install for the parent package winetricks.
	* ., Makefile.am, VERSION.av, configure.ac, debian/control,
	  debian/control.m4, winetricks-packages: convert winetricks
	  package to debian packages
	* ., AUTHORS, ChangeLog, Makefile.am, NEWS, README, VERSION.av,
	  configure.ac, debian, debian/control, debian/docs,
	  debian/postinst, debian/prerm, postinst.in, prerm.in: Initial by
	  newdeb
